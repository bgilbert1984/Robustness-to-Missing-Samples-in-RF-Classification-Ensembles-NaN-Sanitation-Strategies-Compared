% Auto-generated; do not edit.

% ---- Global mask characteristics by corruption level ----
\begin{table}[t]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Corruption & $\tilde{f}_\text{NaN}$ & Longest Run & Run Count & $n$ \\
\midrule
{% for r in global_rows -%}
{{ "{:.0f}".format(100*r.ratio) }}\% & {{ r.nan_fraction_median | snum(fmt="{:.3f}") }} & {{ r.nan_run_longest_median | snum }} & {{ r.nan_run_count_median | snum }} & {{ r.n | snum(fmt="{:.0f}") }} \\
{% endfor -%}
\bottomrule
\end{tabular}
\caption{Median mask characteristics vs. corruption ratio (modes collapsed). $\tilde{f}_\text{NaN}$: median NaN fraction.}
\label{tab:mask_global}
\end{table}

% ---- Per-SNR mask characteristics at focal corruption level ----
\begin{table}[t]
\centering
\small
\begin{tabular}{lcccc}
\toprule
SNR bin @ {{ "{:.0f}".format(100*focal) }}\% & $\tilde{f}_\text{NaN}$ & Longest Run & Run Count & $n$ \\
\midrule
{% for r in focal_snr -%}
{{ r.snr_bin | slabel }} & {{ r.nan_fraction_median | snum(fmt="{:.3f}") }} & {{ r.nan_run_longest_median | snum }} & {{ r.nan_run_count_median | snum }} & {{ r.n | snum(fmt="{:.0f}") }} \\
{% endfor -%}
\bottomrule
\end{tabular}
\caption{Median mask characteristics by SNR bin at the focal corruption level.}
\label{tab:mask_snr_focal}
\end{table}